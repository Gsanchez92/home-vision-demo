package service;

import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
public class HouseServiceTest {
//TODO: Testing here would include the testing of the webclient. When dealing with WebClient in unit tests, things can get tricky because of how it's set up with method chaining.
// If you're using a mock of WebClient, you have to specify how each method call behaves, otherwise you'll end up with null values, one null value can break the whole chain and cause problems which are hard to identify
}
